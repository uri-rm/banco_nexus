server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router: toda ruta cae en index.html (evita 404 al refrescar)
    location / {
        try_files $uri $uri/ /index.html;
    }
}